Flask==3.1.1
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.1.0
SQLAlchemy==2.0.40
alembic==1.15.2
playwright==1.52.0
Pillow==11.2.1
numpy==2.2.5
python-dotenv==1.1.0
Werkzeug==3.1.3

# --- Optional dependencies (install separately if needed) ---
# ndi-python>=5.1.1      # NDI output — requires NDI SDK; app runs in dummy mode without it
# psycopg2-binary==2.9.10  # PostgreSQL driver — only needed if using PostgreSQL instead of SQLite
